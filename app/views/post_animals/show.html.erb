<h2>投稿詳細</h2>

<div>
    <%= image_tag @post_animal.get_image %>
    <%= @post_animal.caption %>
    <%= @post_animal.created_at.strftime('%Y/%m/%d') %>
    <% if@post_animal.user_id == current_user.id %>
      <%= link_to "削除", post_animal_path, method: :delete %>
    <% end %>
</div>