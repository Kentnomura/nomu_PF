<div class="container">
  <div class="row mt-2">
    <h2 class="col-3 head-line title_h2 bg-secondary text-white text-center">フォロー　一覧</h2>
  </div>
</div>


<% if @users.exists? %>
  <div class="row mt-3">
    <div class="col-lg-8 offset-2">
      <table class="table text-center table-hover">
        <thead class="table-danger">
          <tr class="text-center">
            <th>
              user画像
            </th>
            <th>
              ニックネーム
            </th>
            <th>
              フォロー数
            </th>
            <th>
              フォロワー数
            </th>
            <th>
              フォロー状態
            </th>
          </tr>
        </thead>
        <% @users.each do |user| %>
          <tbody class="table-success text-center">
            <tr>
              <th>
                <%= image_tag user.get_profile_image, size: '100x100' %>
              </th>
              <th class="pt-5">
                <%= user.name %>
               </th>
               <th class="pt-5">
                フォロー数: <%= user.followings.count %>
              </th>
              <th class="pt-5">
                フォロワー数: <%= user.followers.count %>
              </th>
              <th class="pt-5">
                <% if current_user != user %>
                  <% if current_user.following?(user) %>
                    <%= link_to "フォロー外す", user_relationships_path(user.id), method: :delete %>
                  <% else %>
                    <%= link_to "フォローする", user_relationships_path(user.id), method: :post %>
                  <% end %>
                <% end %>
              </th>
            </tr>
          </tbody>
        <% end %>
      </table>
    </div>
  </div>
<% else %>
  <div class="container">
    <div class="row mt-2">
      <div class="col text text-center text-white">
        <h2>フォローは、いません</h2>
      </div>
    </div>
  </div>
<% end %>