<tr>
  <%= form_with(model:[post_animal, animal_comment], local: false) do |f| %>
    <th>
      <%= f.text_area :comment, rows: '4', placeholder: "コメントをここに" %>
    </th>
    <th>
      <%= f.submit "送信する", class:"btn btn-outline-primary mt-5" %>
    </th>
  <% end %>
</tr>