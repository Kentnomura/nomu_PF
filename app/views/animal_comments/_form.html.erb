<tr>
  <%= form_with(model:[post_animal, animal_comment], local: false) do |f| %>
    <th>
      <%= f.text_area :comment, rows: '4', placeholder: "コメントをここに(最大40)" %>
    </th>
    <th>
      <%= f.submit "送信する", class:"btn btn-light mt-5" %>
    </th>
  <% end %>
</tr>